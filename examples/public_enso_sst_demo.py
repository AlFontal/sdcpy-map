"""Minimal runnable demo for sdcpy-map."""

from pathlib import Path

from sdcpy_map.cli import main

if __name__ == "__main__":
    # CLI defaults can be overridden by direct command-line execution.
    main()
